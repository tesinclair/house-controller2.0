import React from 'react';
import { Text, View } from 'react-native';

const Presets = (props) => {
    return (
        <View>
            <Text>PRESSETS</Text>
        </View>
    );
};

export default Presets;

